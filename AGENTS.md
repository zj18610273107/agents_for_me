# AGENTS.md

版本：0.0.1

## 工作方式

- 默认中文回答；涉及 Linux、C、内核、嵌入式、驱动时解释准确、简洁，不啰嗦。
- 用户粘贴的 Linux 命令不执行，提示“你可能把 Linux 命令输到 Codex 里了”。
- 不主动编译、测试、运行程序，除非用户明确确认, 可以给出命令建议，但默认不替用户执行。
- 只做用户要求做的事情, 做完等待用户指令。
- 全托管项目白名单中的项目，视为用户只看结果、不关注具体实现过程, 可以连续完成分析、修改、必要验证和总结。
- 尽量少写代码：先复用已有接口，能少改就不大改。
- 提出代码修改建议先说明修改逻辑, 再给伪代码, 再给关键diff。
- C/Linux/嵌入式代码使用 Linux kernel 风格：职责单一、命名清楚、错误路径清晰、资源申请和释放成对出现、避免复杂宏和隐藏副作用。
- 空实现、stub、mock 必须用有明确说明的 `TODO` 标注，不能只写一个空泛的 `TODO`。

## 低资源与文件创建

- 默认按低资源模式工作，尽量减少磁盘写入、CPU 占用和内存占用，不启动非必要的后台任务、文件监听或持续采样。
- 除非完成当前任务确有必要，否则不创建日志、缓存、临时文件、备份、副本、数据库、索引、报告、下载文件或其他生成物；优先复用已有文件，并将诊断结果输出到终端。
- 如果用户要求的任务确实需要占用较多 CPU、内存、磁盘或网络资源，或者需要创建较多缓存、临时文件及其他生成物，执行前必须说明必要性、预计资源占用、文件路径和清理方式，并获得用户明确确认。
- 不主动启用 `TRACE`、`DEBUG` 等高频持久化日志，不把大量命令输出重定向到文件。
- 编译、测试或运行可能生成缓存、构建目录及其他文件时，即使项目处于全托管模式，也要先说明用途、目标路径和预计影响，并获得用户确认。
- 必须创建文件时，只创建完成任务所需的最少文件；提前说明原因、路径和清理方式，任务结束后不得遗留无用的临时产物。

## 全托管项目白名单

当前项目名或仓库根目录名命中以下任一项时，启用全托管模式：
- `A_info_Report`
- `self-runbook`
- `codex_code_selected`
- `common_skills`
- `agents_for_me`

## 提交规则

- 当用户要求创建或整理 git commit 时，所有提交信息末尾都加上 `Signed-off-by: zhou jie <zj18610273107@163.com>`。
- 如果提交内容由 Codex 参与分析、修改或生成，则额外加上 `Co-authored-by: Codex <codex@openai.com>` 作为 Codex 署名。
- 务必记得多个提交署名连续书写，中间不加空行。
- 以下仓库署名不能加 codex:
    `smiOS`

## 输出

- 命令和代码放独立代码块，不加 `$`、`#`、`>` 提示符；多条命令放同一个代码块；解释写在代码块外。
- 一条完整命令不要因为显示宽度被人为拆行；如果确实需要输出多行命令，必须在每个续行末尾明确添加 `\`，并使用符合对应 Shell 语法的续行方式，保证复制后仍可直接执行。
- 引用代码位置或具体行号时，必须单独占一行，统一写成 `relative/path/file.c:141`；文件路径、英文冒号和行号必须连续，中间不能有空格或换行。禁止写成 `src/main.c 141`、`src/main.c 第 141 行`、`src/main.c: line 141` 或孤立的 `:141`。
- 展示 diff 或带行号的代码片段时，在代码块前单独输出该片段起始位置，格式仍为 `relative/path/file.c:141`，使 VS Code 终端可以通过 `Ctrl+单击` 跳转；不要依赖代码块内孤立的行号完成跳转。
- 多行文件内容默认在代码块前单独标注起始位置，代码块内不重复文件路径；只有明确需要逐行跳转时，才将每一行写成 `relative/path/file.c:141 | 内容` 格式。行号前必须有英文冒号，禁止只写 `141 | 内容` 或 `:141 | 内容`。
- 完整引用函数签名时，将 `#` 紧贴函数名，例如 `static int #foo (int value)`；单独提及函数名时使用 `#foo`，并与前后文本保留空格；只要涉及函数签名或函数名，必须使用 Markdown 行内代码包裹，以便显示为蓝色并支持 VS Code 定位。

<!-- CODEGRAPH_START -->
## CodeGraph

在已建立 CodeGraph 索引的仓库中（仓库根目录存在 `build/codegraph.db`），理解或定位代码时，应先使用 CodeGraph，再使用 grep/find/read：

- **MCP 工具**（可用时）：`codegraph_explore` 通常可通过一次调用回答大多数代码问题，并返回原始源码及调用路径。如果该工具已列出但处于延迟加载状态，请通过工具搜索按名称加载。
- **Shell**（始终可用）：`CODEGRAPH_DIR=build codegraph explore "<符号名称或问题>"`。如果直接运行 `codegraph ...` 时提示不存在 `.codegraph/` 索引，不要运行 `codegraph init`；应设置 `CODEGRAPH_DIR=build` 后重试。

如果不存在 `build/codegraph.db`，且已配置的其他 `CODEGRAPH_DIR` 也未指向现有数据库，则跳过 CodeGraph，并提示用户需要建立索引；不要自行建立索引。
<!-- CODEGRAPH_END -->
